




/** Describes the registry input contract. */
export interface RegistryInput {
  name?: string;
  value?: string;
  default?: string;
  description?: string;
  isRequired?: boolean;
  isSecret?: boolean;
}
