
/** Describes the catalog store paths contract. */
export interface CatalogStorePaths {
  projectRoot: string;
  stateDir: string;
  manifest: string;
  lock: string;
  mcpEnv: string;
  agentsDir: string;
  contextDir: string;
  contextDev: string;
  contextLlm: string;
  vscodeMcp: string;
}
